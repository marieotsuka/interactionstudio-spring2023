---
title: "Projects"
layout: default
eleventyExlcudeFromCollections: true
---

<div class="projects">
	<h1>{{ title }}</h1>
	<p>
		We will be exploring four project themes over the course of the semester. 
	</p>
	<section class="link-list">
	{% assign projects = collections.project %}
	{% for p in projects %}
		<a href="{{p.url}}" class="link-button">
			<div class="project">
				<div class="meta">Unit {{p.data.unit}}</div>
				{{p.data.title}}
			</div>
		</a>
	{% endfor %}
	</section>
</div>	